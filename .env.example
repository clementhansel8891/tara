# ==============================================================================
# TARA HR System — Environment Variables
# Copy this file to .env and adjust values for your deployment.
#
# Usage:
#   cp .env.example .env
#   docker compose up --build
# ==============================================================================

# --- Database ---
DB_PASSWORD=tara_secret_2024
DB_PORT=5432

# --- Redis ---
REDIS_PORT=6379

# --- Backend ---
BACKEND_PORT=3001
JWT_SECRET=change-this-to-a-long-random-string-in-production
ALLOWED_ORIGINS=http://localhost,http://localhost:5173

# --- Frontend ---
FRONTEND_PORT=80
VITE_API_URL=http://localhost:3001

# --- Hermes Agentic AI Integration ---
# Set to true to enable Hermes on first boot (can also be enabled via admin API)
HERMES_ENABLED=false
# API key that Hermes uses to authenticate (generate a strong random key)
HERMES_API_KEY=
